import { LoadingTextPipe } from './loading-text.pipe';

describe('LoadingTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LoadingTextPipe();
    expect(pipe).toBeTruthy();
  });
});
