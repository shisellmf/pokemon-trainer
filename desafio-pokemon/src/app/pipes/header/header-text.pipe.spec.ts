import { HeaderTextPipe } from './header-text.pipe';

describe('HeaderTextPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderTextPipe();
    expect(pipe).toBeTruthy();
  });
});
